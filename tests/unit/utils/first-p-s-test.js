import firstPS from 'dummy/utils/first-p-s';
import { module, test } from 'qunit';

module('Unit | Utility | first-p-s', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = firstPS();
    assert.ok(result);
  });
});
